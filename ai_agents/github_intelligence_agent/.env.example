# Google Gemini API key (required). Get one free at https://aistudio.google.com
GEMINI_API_KEY=your_gemini_api_key_here

# GitHub Personal Access Token (required). Generate one at GitHub Settings > Developer Settings > Personal Access Tokens > Fine-grained.
# Read-only access to public repositories is enough.
GITHUB_PAT=your_github_pat_here
